name: Create Release Version

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (for example 0.1.0 or 1.0.0-alpha.0)"
        required: true
        type: string
      prerelease:
        description: "Mark the GitHub release as a prerelease"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  release:
    uses: clevertask/clevertask-public-workflows/.github/workflows/create-release-version.yml@77cd7e4a24bfc9debc74ff3082bc36c2894bb04e
    with:
      version: ${{ inputs.version }}
      prerelease: ${{ inputs.prerelease }}
    secrets:
      github_pat: ${{ secrets.CLEVERTASK_PAT }}
